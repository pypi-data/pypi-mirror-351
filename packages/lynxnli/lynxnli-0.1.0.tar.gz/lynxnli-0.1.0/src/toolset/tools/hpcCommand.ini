[tool]
name = hpcCommand
address = hpcCommand.hpcCommand:run
inputs = command
outputs = output_file_path
instructions = to answer the question for hpc cluster, please generate the corresponding terminal command for the question. Using && for multiple commands. Please only return the commands without any comments and separate them with &&.
