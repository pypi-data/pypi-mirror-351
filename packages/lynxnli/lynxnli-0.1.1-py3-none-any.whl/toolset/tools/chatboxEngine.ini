[tool]
name = chatboxEngine
# Address: module path and function name
address = chatboxEngine.chatboxEngine:run
inputs = text
outputs = response
instructions = The tool is to answer some user qestions with LLM, forwards incoming qestion text and returns the LLM's response
extraction.text = (.+)