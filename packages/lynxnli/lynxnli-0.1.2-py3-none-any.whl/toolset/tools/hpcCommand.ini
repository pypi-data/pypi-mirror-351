[tool]
name = hpcCommand
address = hpcCommand.hpcCommand:run
inputs = command
outputs = output_file_path
instructions = This tool is only for execute commands in hpc cluster, please generate the corresponding terminal command for the question. Using && for multiple commands. Please only return the commands without any comments and separate them with &&.
