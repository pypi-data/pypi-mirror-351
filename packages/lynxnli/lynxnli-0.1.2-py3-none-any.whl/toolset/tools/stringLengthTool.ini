[tool]
name = stringLengthTool
address = stringLengthTool.stringLengthTool:run
inputs = text
outputs = length
instructions = For the given string from user, calucate its length and give it back.
