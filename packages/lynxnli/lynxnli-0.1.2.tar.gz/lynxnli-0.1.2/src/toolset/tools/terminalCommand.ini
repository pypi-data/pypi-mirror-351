[tool]
name = terminalCommand
address = terminalCommand.terminalCommand:run
inputs = code
outputs = execution_result
instructions = This tool is only for my local personal computer, generate the corresponding terminal commands for the given request in my local computer . Always use the newest version for request. Please only return the commands without any comments and separate them with &&. 
extraction.code = run this code:\s*(.+)
