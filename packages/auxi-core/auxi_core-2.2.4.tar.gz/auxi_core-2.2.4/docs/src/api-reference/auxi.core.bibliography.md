# package `auxi.core.bibliography`

Provide auxi's bibliography, and tools to interact with it.

## Type Aliases

`type library`: `bibtexparser.parse_file(str(Path(__file__).parent.parent / 'data' / 'bibliography.bib'))`
