[project]
name = "bandgraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "ipykernel",
    "ipysigma",
    "matplotlib",
    "networkx",
    "pandas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"