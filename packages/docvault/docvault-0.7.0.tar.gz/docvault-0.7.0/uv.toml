# Minimal UV configuration

# Resolution strategy for dependencies
resolution = "highest"

# Use compiled bytecode for performance
# Disabled to prevent "Bytecode compiled" messages on every run
compile-bytecode = false
