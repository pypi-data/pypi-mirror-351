[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/secret"]

[project]
name = "r00secret"
version = "0.1.2"
requires-python = ">=3.12"
dependencies = [
    "r00logger",
    "python-box>=7.3.2",
    "ruamel.yaml"

]

