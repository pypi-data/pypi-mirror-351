[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paratkinter"
version = "1.1.0"
description = "Add parallax effects and UI enhancements to (custom)tkinter apps"
readme = "README.md"
authors = [{ name = "Kabir Tiwari", email = "kabir.tiwari@outlook.com" }]
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["customtkinter"]

