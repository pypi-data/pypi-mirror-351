[project]
name = "hieuvision"
version = "0.2.0"
description = "A modular computer vision toolkit with augmentation and more."
authors = [
  { name = "Hieu Nguyen", email = "nguyenminhhieudn02@gmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.6"
dependencies = [
  "opencv-python",
  "albumentations",
  "tqdm"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
