[build-system]
requires      = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name    = "MetaPreprocessor"
version = "0.0.4"
authors = [
    { name = "Phuc X. Doan", email = "phucxdoan@gmail.com" },
]
description     = "Generate C code using Python code embedded within C source files."
requires-python = ">=3.14"
license         = "MIT"
license-files   = ["LICEN[CS]E*"]
