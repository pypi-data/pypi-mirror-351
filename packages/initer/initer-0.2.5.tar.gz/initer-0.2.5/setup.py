from setuptools import setup, find_packages

setup(
    name='initer',
    version='0.2.5',
    description='A Python lib you can use to initialize your Python scripts (Console based / Windowed)',
    author='Lilou Palagier',
    packages=find_packages()
)
