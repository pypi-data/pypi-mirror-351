# This file is generated by SciPy's build process
# It contains system_info results at the time of building this package.
from enum import Enum

__all__ = ["show"]
_built_with_meson = True


class DisplayModes(Enum):
    stdout = "stdout"
    dicts = "dicts"


def _cleanup(d):
    """
    Removes empty values in a `dict` recursively
    This ensures we remove values that Meson could not provide to CONFIG
    """
    if isinstance(d, dict):
        return { k: _cleanup(v) for k, v in d.items() if v != '' and _cleanup(v) != '' }
    else:
        return d


CONFIG = _cleanup(
    {
        "Compilers": {
            "c": {
                "name": "clang",
                "linker": r"ld.lld",
                "version": "18.0.2",
                "commands": r"aarch64-linux-android-clang",
                "args": r"-fstack-protector-strong, -Oz, -I/data/data/org.qpython.plus/files/include",
                "linker args": r"-L/data/data/org.qpython.plus/files/lib, -Wl,-rpath=/data/data/org.qpython.plus/files/lib, -Wl,--enable-new-dtags, -Wl,--as-needed, -Wl,-z,relro,-z,now, -Wl,--as-needed,-lpython3.12",
            },
            "cython": {
                "name": r"cython",
                "linker": r"cython",
                "version": r"3.0.11",
                "commands": r"cython",
                "args": r"",
                "linker args": r"",
            },
            "c++": {
                "name": "clang",
                "linker": r"ld.lld",
                "version": "18.0.2",
                "commands": r"aarch64-linux-android-clang++",
                "args": r"-fstack-protector-strong, -Oz, -I/data/data/org.qpython.plus/files/include",
                "linker args": r"-L/data/data/org.qpython.plus/files/lib, -Wl,-rpath=/data/data/org.qpython.plus/files/lib, -Wl,--enable-new-dtags, -Wl,--as-needed, -Wl,-z,relro,-z,now, -Wl,--as-needed,-lpython3.12",
            },
            "fortran": {
                "name": "flang",
                "linker": r"ld.lld",
                "version": "18.0.2",
                "commands": r"aarch64-linux-android-flang-new",
                "args": r"",
                "linker args": r"-L/data/data/org.qpython.plus/files/lib, -Wl,-rpath=/data/data/org.qpython.plus/files/lib, -Wl,--enable-new-dtags, -Wl,--as-needed, -Wl,-z,relro,-z,now, -Wl,--as-needed,-lpython3.12",
            },
            "pythran": {
                "version": r"",
                "include directory": r""
            },
        },
        "Machine Information": {
            "host": {
                "cpu": r"aarch64",
                "family": r"aarch64",
                "endian": r"little",
                "system": r"android",
            },
            "build": {
                "cpu": r"x86_64",
                "family": r"x86_64",
                "endian": r"little",
                "system": r"linux",
            },
            "cross-compiled": bool("True".lower().replace('false', '')),
        },
        "Build Dependencies": {
            "blas": {
                "name": "openblas",
                "found": bool("True".lower().replace('false', '')),
                "version": "0.3.28",
                "detection method": "pkgconfig",
                "include directory": r"/data/data/org.qpython.plus/files/include/openblas",
                "lib directory": r"/data/data/org.qpython.plus/files/lib",
                "openblas configuration": r"USE_64BITINT= NO_CBLAS= NO_LAPACK= NO_LAPACKE= DYNAMIC_ARCH=OFF DYNAMIC_OLDER=OFF NO_AFFINITY=1 USE_OPENMP= ARMV8 MAX_THREADS=0",
                "pc file directory": r"/data/data/org.qpython.plus/files/lib/pkgconfig",
            },
            "lapack": {
                "name": "openblas",
                "found": bool("True".lower().replace('false', '')),
                "version": "0.3.28",
                "detection method": "pkgconfig",
                "include directory": r"/data/data/org.qpython.plus/files/include/openblas",
                "lib directory": r"/data/data/org.qpython.plus/files/lib",
                "openblas configuration": r"USE_64BITINT= NO_CBLAS= NO_LAPACK= NO_LAPACKE= DYNAMIC_ARCH=OFF DYNAMIC_OLDER=OFF NO_AFFINITY=1 USE_OPENMP= ARMV8 MAX_THREADS=0",
                "pc file directory": r"/data/data/org.qpython.plus/files/lib/pkgconfig",
            },
            "pybind11": {
                "name": "pybind11",
                "version": "2.13.6",
                "detection method": "config-tool",
                "include directory": r"unknown",
            },
        },
        "Python Information": {
            "path": r"/home/builder/.termux-build/python3.12-crossenv-prefix-bionic-aarch64/cross/bin/python",
            "version": "3.12",
        },
    }
)


def _check_pyyaml():
    import yaml

    return yaml


def show(mode=DisplayModes.stdout.value):
    """
    Show libraries and system information on which SciPy was built
    and is being used

    Parameters
    ----------
    mode : {`'stdout'`, `'dicts'`}, optional.
        Indicates how to display the config information.
        `'stdout'` prints to console, `'dicts'` returns a dictionary
        of the configuration.

    Returns
    -------
    out : {`dict`, `None`}
        If mode is `'dicts'`, a dict is returned, else None

    Notes
    -----
    1. The `'stdout'` mode will give more readable
       output if ``pyyaml`` is installed

    """
    if mode == DisplayModes.stdout.value:
        try:  # Non-standard library, check import
            yaml = _check_pyyaml()

            print(yaml.dump(CONFIG))
        except ModuleNotFoundError:
            import warnings
            import json

            warnings.warn("Install `pyyaml` for better output", stacklevel=1)
            print(json.dumps(CONFIG, indent=2))
    elif mode == DisplayModes.dicts.value:
        return CONFIG
    else:
        raise AttributeError(
            f"Invalid `mode`, use one of: {', '.join([e.value for e in DisplayModes])}"
        )
