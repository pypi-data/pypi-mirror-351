[project]
name = "package-finder"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hao Wu", email = "haowu@dataset.sh" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "tqdm>=4.67.1",
]

[project.scripts]
package-finder = "package_finder.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
