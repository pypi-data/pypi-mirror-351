[project]
name = "cidrtool"
version = "0.1.0"
description = ""
authors = [
    {name = "laixintao",email = "laixintaoo@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click (>=8.1.8,<9.0.0)",
    "netaddr (>=1.3.0,<2.0.0)"
]

[tool.poetry.scripts]
cidrtool = 'cidrtool.main:cidrtool'


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
