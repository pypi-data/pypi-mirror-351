# Due to multiple errors with the official azurite image, we are using the azurite CLI instead
FROM node

RUN npm install -g azurite

CMD azurite --debug /azurite/debug.log --inMemoryPersistence --blobHost 0.0.0.0
