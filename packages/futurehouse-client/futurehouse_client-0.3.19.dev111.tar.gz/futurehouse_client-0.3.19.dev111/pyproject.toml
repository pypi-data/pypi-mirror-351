[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=64", "setuptools_scm>=8"]

[project]
authors = [
    {email = "hello@futurehouse.org", name = "FutureHouse technical staff"},
]
classifiers = [
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python",
]
dependencies = [
    "cloudpickle",
    "dm-tree<0.1.9",  # Pin to have macOS wheels and avoid requiring cmake: https://github.com/google-deepmind/tree/issues/128
    "fhaviary",
    "httpx",
    "ldp>=0.22.0",
    "litellm==1.67.4.post1",  # TODO: remove after: https://github.com/BerriAI/litellm/issues/10349
    "pydantic",
    "python-dotenv",
    "tenacity",
    "tqdm>=4.62",  # For tqdm.asyncio stdlib consistency
]
description = "A client for interacting with endpoints of the FutureHouse service."
dynamic = ["version"]
name = "futurehouse-client"
readme = "README.md"
requires-python = ">=3.11,<3.13"  # Pin to have dm-tree macOS wheels and avoid requiring cmake

[project.optional-dependencies]
dev = [
    "black",
    "jupyter",
    "jupyterlab",
    "mypy",
    "notebook",
    "pre-commit",
    "pylint",
    "pylint-per-file-ignores",
    "pylint-pydantic",
    "pytest",
    "pytest-asyncio",
    "pytest-rerunfailures",
    "pytest-subtests",
    "pytest-timeout",
    "pytest-xdist",
    "ruff",
    "setuptools_scm",
]
monitoring = [
    "newrelic>=8.8.0",
]

[tool.setuptools]
license-files = []

[tool.setuptools.packages.find]
exclude = ["tests"]
include = ["futurehouse_client*"]

[tool.setuptools_scm]
local_scheme = "no-local-version"
root = "../.."
version_file = "version.py"

[[tool.uv.index]]
explicit = true
name = "testpypi"
publish-url = "https://test.pypi.org/legacy/"
url = "https://test.pypi.org/"
