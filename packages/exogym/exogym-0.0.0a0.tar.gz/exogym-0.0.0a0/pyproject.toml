[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "exogym"
version = "0.0.0a0"
description = "Simulate distributed training on local devices"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Matt Beton", email = "matthew.beton@gmail.com" }]
classifiers = [
    "Programming Language :: Python :: 3"
]

