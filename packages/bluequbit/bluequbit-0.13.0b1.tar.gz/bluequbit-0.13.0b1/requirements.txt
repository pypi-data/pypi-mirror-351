requests~=2.28
python-dateutil~=2.8
# We can't bump this NumPy version until the lowest common denominator of our stack,
# task runner GPU gets updated. i.e. until NVIDIA updates their cuquantum appliance
numpy>=1.21
packaging
