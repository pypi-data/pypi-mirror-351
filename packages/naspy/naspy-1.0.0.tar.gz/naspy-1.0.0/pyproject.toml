[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "naspy"
version = "1.0.0"
description = "A lightweight memory protocol inspired by cognitive structure"
readme = "README.md"
authors = [
  { name="Beelito", email="beelito.dev@gmail.com" }
]
license = { text = "MIT" }
dependencies = []
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/beelito/naspy"
