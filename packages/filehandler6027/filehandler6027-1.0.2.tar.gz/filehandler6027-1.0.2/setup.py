from setuptools import setup, find_packages

setup(
    name='filehandler6027',
    version='1.0.0',
    author='Charlie Sky',
    description='Flexible file handling utilities with encoding, platform safety, and logging',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
